import { AppShell } from '@/components/layout/app-shell';

export default function SignupPage() {
  return (
    <AppShell title="회원가입" subtitle="기본 계정 + 소셜 계정 온보딩">
      <div className="rounded-2xl border border-slate-200 bg-white p-6">회원가입 화면 초기 버전</div>
    </AppShell>
  );
}
